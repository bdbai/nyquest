name: Test backends (async)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  test-async-stream-linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Test curl
        run: cargo test -p nyquest-backend-tests --verbose --features async-stream,curl,multipart

  test-async-stream-windows-winrt:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - name: Test winrt
        run: cargo test -p nyquest-backend-tests --verbose --features async-stream,winrt,multipart

  test-async-stream-windows-winhttp:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - name: Test winhttp
        run: cargo test -p nyquest-backend-tests --verbose --features async-stream,winhttp,multipart

  test-async-stream-macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
      - name: Test nsurlsession
        run: cargo test -p nyquest-backend-tests --verbose --features async-stream,nsurlsession,multipart

  test-async-stream-reqwest:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Test reqwest
        run: cargo test -p nyquest-backend-tests --verbose --features async-stream,reqwest,multipart
